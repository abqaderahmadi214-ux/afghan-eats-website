const AEMERCHANT={map:new Map(),timer:null};
function merchantText(en,fa){return typeof lang!=='undefined'&&lang==='fa'?fa:en}
function merchantEsc(v){return String(v??'').replaceAll('&','&amp;').replaceAll('<','&lt;').replaceAll('>','&gt;').replaceAll('"','&quot;').replaceAll("'",'&#039;')}
function availabilityLabel(a){if(!a)return'';const labels={inactive:['Unavailable','در دسترس نیست'],merchant_closed:['Closed by restaurant','بسته توسط رستورانت'],temporarily_paused:['Temporarily paused','موقتاً متوقف'],outside_opening_hours:['Closed now','اکنون بسته'],capacity_reached:['At capacity','ظرفیت تکمیل']};if(a.acceptingNow){if(a.operationMode==='very_busy')return merchantText('Very busy','بسیار مصروف');if(a.operationMode==='busy')return merchantText('Busy','مصروف');return merchantText('Open now','اکنون باز')}const x=labels[a.reason]||['Not accepting orders','سفارش نمی‌پذیرد'];return merchantText(x[0],x[1])}
function applyAvailabilityToRestaurant(r,a){if(!r||!a)return;r._availability=a;if(r._baseEtaMin===undefined)r._baseEtaMin=r.delivery_time_min;if(r._baseEtaMax===undefined)r._baseEtaMax=r.delivery_time_max;r.is_open=Boolean(a.acceptingNow);const b=Number(a.etaBufferMin||0);if(r._baseEtaMin!=null)r.delivery_time_min=Number(r._baseEtaMin)+b;if(r._baseEtaMax!=null)r.delivery_time_max=Number(r._baseEtaMax)+b}
async function refreshMarketplaceAvailability(){if(typeof restaurants==='undefined'||!restaurants.length||typeof trpcQuery!=='function')return;try{const data=await trpcQuery('merchant.publicAvailabilityBatch',{restaurantIds:restaurants.map(r=>r.id).filter(Boolean).slice(0,100)});for(const a of data||[]){AEMERCHANT.map.set(a.restaurantId,a);const r=restaurants.find(x=>x.id===a.restaurantId);applyAvailabilityToRestaurant(r,a)}if(typeof renderRestaurantList==='function')renderRestaurantList();if(document.getElementById('homeRestaurants')&&typeof renderCards==='function')renderCards('#homeRestaurants',restaurants.filter(r=>r.status==='active').slice(0,6));decorateCurrentMerchant()}catch(e){console.warn('Merchant availability unavailable',e)}}
function merchantStatusBanner(a){if(!a)return'';const detail=a.acceptingNow?(a.operationMode==='normal'?merchantText('Accepting orders normally.','سفارش‌ها به‌صورت عادی پذیرفته می‌شوند.'):merchantText(`Kitchen is ${a.operationMode.replace('_',' ')}; ETA includes a live delay.`,`آشپزخانه ${a.operationMode==='busy'?'مصروف':'بسیار مصروف'} است؛ زمان تحویل با تأخیر زنده محاسبه شده است.`)):(a.reason==='capacity_reached'?merchantText('The kitchen has reached its current order capacity. You can try again shortly or schedule for later.','آشپزخانه به ظرفیت فعلی رسیده است. کمی بعد دوباره تلاش کنید یا برای بعد زمان‌بندی کنید.'):a.reason==='temporarily_paused'?merchantText(`New orders are temporarily paused${a.pauseReason?`: ${a.pauseReason}`:''}.`،`سفارش جدید موقتاً متوقف است${a.pauseReason?`: ${a.pauseReason}`:''}.`):merchantText('This restaurant is currently closed for immediate orders.','این رستورانت فعلاً سفارش فوری نمی‌پذیرد.'));return`<div class="ae-merchant-banner ${a.acceptingNow?'open':'closed'}"><b>${merchantEsc(availabilityLabel(a))}</b><span>${merchantEsc(detail)}</span>${!a.acceptingNow&&a.acceptingScheduled?`<small>${merchantText('Scheduled ordering may still be available.','سفارش زمان‌بندی‌شده ممکن است فعال باشد.')}</small>`:''}</div>`}
function decorateCurrentMerchant(){if(typeof currentRestaurant==='undefined'||!currentRestaurant)return;const a=AEMERCHANT.map.get(currentRestaurant.id)||currentRestaurant._availability;if(!a)return;let el=document.getElementById('aeMerchantBanner');if(!el){el=document.createElement('div');el.id='aeMerchantBanner';const hero=document.getElementById('storeHero');if(hero)hero.insertAdjacentElement('afterend',el);else document.querySelector('main')?.prepend(el)}el.innerHTML=merchantStatusBanner(a);if(typeof currentMenuItems!=='undefined')for(const item of currentMenuItems){if(item.is_available!==false)continue;document.querySelectorAll('.menu-item .add-btn').forEach(btn=>{const code=btn.getAttribute('onclick')||'';if(code.includes(`'${item.id}'`)){btn.disabled=true;btn.textContent='×';const row=btn.closest('.menu-item');if(row&&!row.querySelector('.ae-soldout')){const s=document.createElement('span');s.className='ae-soldout';s.textContent=item.unavailable_reason||merchantText('Temporarily sold out','موقتاً ناموجود');row.querySelector('div')?.appendChild(s)}}})}}
function installMenuAvailabilityWrapper(){if(typeof window.getMenu!=='function'||window.getMenu.__aeMerchant)return;const original=window.getMenu;const wrapped=async function(r){const menu=await original(r);try{const states=await trpcQuery('merchant.publicMenuAvailability',{restaurantId:r.id}),map=new Map((states||[]).map(x=>[x.id,x]));menu.items=(menu.items||[]).map(item=>{const s=map.get(item.id);return s?{...item,is_available:Boolean(s.effectiveAvailable),unavailable_until:s.unavailableUntil,unavailable_reason:s.reason}:item});}catch(e){console.warn('Temporary stock status unavailable',e)}return menu};wrapped.__aeMerchant=true;window.getMenu=wrapped}
function installItemGuard(){if(typeof window.openItem==='function'&&!window.openItem.__aeMerchant){const original=window.openItem;const wrapped=function(rid,iid){const item=(typeof currentMenuItems!=='undefined'?currentMenuItems:[]).find(x=>x.id===iid);if(item&&item.is_available===false){alert(item.unavailable_reason||merchantText('This item is temporarily sold out.','این قلم موقتاً ناموجود است.'));return}return original(rid,iid)};wrapped.__aeMerchant=true;window.openItem=wrapped}}
async function checkoutMerchantAvailability(scheduledFor=null){const rid=(typeof cart!=='undefined'&&cart[0]?.restaurantId)||null;if(!rid)return null;const input={restaurantId:rid};if(scheduledFor)input.at=scheduledFor;try{return await trpcQuery('merchant.publicAvailability',input)}catch{return null}}
function installCheckoutGuard(){if(typeof window.placeOrder!=='function'||window.placeOrder.__aeMerchant)return;const original=window.placeOrder;const wrapped=async function(e){const form=e.currentTarget||e.target,mode=String(new FormData(form).get('scheduleMode')||'asap'),later=mode==='later',raw=String(new FormData(form).get('scheduledFor')||'');let iso=null;if(later&&raw){const d=new Date(raw);if(!Number.isNaN(d.getTime()))iso=d.toISOString()}const a=await checkoutMerchantAvailability(iso);if(a){const allowed=later?(a.acceptingScheduled&&(!a.scheduleConfigured||a.scheduleOpen)&&(!a.pausedUntil||!iso||new Date(a.pausedUntil).getTime()<=new Date(iso).getTime())):a.acceptingNow;if(!allowed){e.preventDefault();const er=document.getElementById('checkoutError');if(er){er.textContent=later?merchantText('The restaurant cannot accept an order at that scheduled time. Choose another time.','رستورانت در زمان انتخاب‌شده سفارش نمی‌پذیرد. زمان دیگری انتخاب کنید.'):availabilityLabel(a);er.classList.remove('hidden')}return}}return original(e)};wrapped.__aeMerchant=true;window.placeOrder=wrapped}
function merchantStyles(){if(document.getElementById('aeMerchantStyles'))return;const s=document.createElement('style');s.id='aeMerchantStyles';s.textContent=`.ae-merchant-banner{max-width:1180px;margin:14px auto 0;padding:14px 18px;border-radius:15px;display:flex;gap:10px;align-items:center;flex-wrap:wrap}.ae-merchant-banner.open{background:#eef9f1;color:#17643a;border:1px solid #cfead7}.ae-merchant-banner.closed{background:#fff4ef;color:#8a382c;border:1px solid #f2d4ca}.ae-merchant-banner b{font-size:.95rem}.ae-merchant-banner span{font-size:.9rem}.ae-merchant-banner small{margin-inline-start:auto}.ae-soldout{display:inline-block;margin-top:8px;padding:4px 8px;border-radius:999px;background:#fff0ef;color:#9f2f29;font-size:.75rem;font-weight:800}.restaurant-card .status-pill.busy{background:#fff4d8;color:#774c00}`;document.head.appendChild(s)}
function bootstrapMerchant(){merchantStyles();installMenuAvailabilityWrapper();installItemGuard();installCheckoutGuard();let tries=0;const wait=setInterval(()=>{installMenuAvailabilityWrapper();installItemGuard();installCheckoutGuard();tries++;if((typeof restaurants!=='undefined'&&restaurants.length)||tries>20){clearInterval(wait);refreshMarketplaceAvailability();setTimeout(decorateCurrentMerchant,250)}},200)}
document.addEventListener('DOMContentLoaded',bootstrapMerchant);if(document.readyState!=='loading')bootstrapMerchant();document.addEventListener('visibilitychange',()=>{if(!document.hidden)refreshMarketplaceAvailability()});AEMERCHANT.timer=setInterval(()=>{if(!document.hidden)refreshMarketplaceAvailability()},60000);
