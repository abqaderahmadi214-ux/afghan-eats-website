const AEFINANCE={overview:null,preview:null,adminLoaded:false,portalLoaded:false};
function finText(en,fa){return typeof lang!=='undefined'&&lang==='fa'?fa:en}
function finEsc(v){return String(v??'').replaceAll('&','&amp;').replaceAll('<','&lt;').replaceAll('>','&gt;').replaceAll('"','&quot;').replaceAll("'",'&#039;')}
function finMoney(v){return `؋ ${Number(v||0).toLocaleString(undefined,{maximumFractionDigits:2})}`}
function finDate(v){if(!v)return'—';try{return new Date(v).toLocaleDateString()}catch{return String(v)}}
function finStyles(){if(document.getElementById('aeFinanceStyles'))return;const s=document.createElement('style');s.id='aeFinanceStyles';s.textContent=`
.finance-warning{padding:13px 15px;border-radius:14px;background:#fff6e8;border:1px solid #f0d7a6;color:#6a4712;margin:12px 0}.finance-grid{display:grid;grid-template-columns:1fr 1fr;gap:14px}.finance-kpis{display:grid;grid-template-columns:repeat(4,minmax(0,1fr));gap:10px;margin:14px 0}.finance-kpi{padding:14px;border:1px solid #e8e4df;border-radius:15px;background:#fff}.finance-kpi small{display:block;color:#777}.finance-kpi strong{display:block;font-size:1.3rem;margin-top:5px}.finance-list{display:grid;gap:10px}.finance-row{border:1px solid #e8e4df;border-radius:14px;padding:13px;background:#fff}.finance-row-head{display:flex;justify-content:space-between;gap:12px;align-items:flex-start}.finance-meta{display:flex;flex-wrap:wrap;gap:8px;color:#777;font-size:.86rem;margin-top:5px}.finance-positive{color:#9b3f1e}.finance-zero{color:#28734b}.finance-actions{display:flex;flex-wrap:wrap;gap:8px;margin-top:10px}.finance-inline-form{display:grid;grid-template-columns:1fr 1fr;gap:8px;margin-top:10px;padding-top:10px;border-top:1px dashed #ddd}.finance-inline-form .wide{grid-column:1/-1}.finance-table{width:100%;border-collapse:collapse;font-size:.9rem}.finance-table th,.finance-table td{padding:8px;border-bottom:1px solid #eee;text-align:left}.finance-preview{padding:16px;border:2px solid #d9493d;border-radius:16px;background:#fffafa;margin-top:14px}.finance-policy{display:flex;flex-wrap:wrap;gap:8px;margin-top:8px}.finance-chip{font-size:.8rem;border-radius:999px;background:#f3f1ef;padding:5px 9px}.finance-portal-card .finance-row{padding:10px}.finance-balance{font-size:1.8rem;font-weight:900;margin:7px 0}@media(max-width:850px){.finance-grid,.finance-kpis{grid-template-columns:1fr}.finance-inline-form{grid-template-columns:1fr}.finance-inline-form .wide{grid-column:auto}.finance-table{display:block;overflow:auto}}
`;document.head.appendChild(s)}
function mountFinanceAdmin(){if(!document.getElementById('adminConsole')||document.getElementById('panelFinance'))return;finStyles();const tabs=document.querySelector('.ops-tabs');if(!tabs)return;const btn=document.createElement('button');btn.className='ops-tab';btn.textContent='Finance & settlements';btn.onclick=function(){showOpsTab('panelFinance',this);loadFinanceAdmin()};tabs.appendChild(btn);const panel=document.createElement('section');panel.id='panelFinance';panel.className='ops-panel hidden';panel.innerHTML=`<div class="ops-panel-head"><div><h2>Cash reconciliation & restaurant settlements</h2><p class="muted">COD custody is recorded automatically for new rider-completed cash deliveries. Settlement economics stay disabled until you explicitly configure them.</p></div><button class="btn btn-light" onclick="loadFinanceAdmin()">↻ Refresh</button></div><div id="financeAdminError" class="notice error hidden"></div><div id="financeKpis" class="finance-kpis"></div><div class="finance-grid"><section class="portal-card"><h3>Settlement policy</h3><div class="finance-warning"><b>No assumed commission.</b><br>Choose who funds discounts, the commission basis, the commission percentage and whether restaurants receive delivery fees before enabling settlements.</div><form id="financeSettingsForm" class="campaign-form" onsubmit="saveFinanceSettings(event)"><label class="switch-row wide"><span>Enable restaurant settlements</span><input name="settlementsEnabled" type="checkbox"></label><input name="restaurantCommissionPercent" type="number" min="0" max="100" step="0.01" placeholder="Commission %"><select name="commissionBasis"><option value="">Commission basis…</option><option value="gross_food">Gross food subtotal</option><option value="net_food">Net food after restaurant-funded discounts</option></select><select name="discountFunder"><option value="">Promotion discount funded by…</option><option value="platform">Afghan Eats / platform</option><option value="restaurant">Restaurant</option></select><select name="restaurantReceivesDeliveryFee"><option value="">Delivery fee ownership…</option><option value="yes">Restaurant receives delivery fee</option><option value="no">Restaurant does not receive delivery fee</option></select><button class="btn btn-primary wide" type="submit">Save finance policy</button></form><div id="financePolicyState" class="ops-security" style="margin-top:12px"></div></section><section class="portal-card"><h3>Create restaurant settlement</h3><form id="settlementPreviewForm" class="campaign-form" onsubmit="previewFinanceSettlement(event)"><select name="restaurantId" required><option value="">Select restaurant…</option></select><input name="startDate" type="date" required><input name="endDate" type="date" required><button class="btn btn-light wide" type="submit">Preview unsettled delivered orders</button></form><div id="financeSettlementPreview"></div></section></div><section class="portal-card" style="margin-top:14px"><h3>Rider COD cash custody</h3><p class="muted">Positive balances mean cash is recorded as held by the rider and awaiting reconciliation. Deposits reduce the balance. This ledger starts with v12; historical deliveries are not backfilled automatically.</p><div id="financeRiders" class="finance-list"></div></section><section class="portal-card" style="margin-top:14px"><h3>Restaurant settlements</h3><div id="financeSettlements" class="finance-list"></div></section><section class="portal-card" style="margin-top:14px"><h3>Recent cash ledger</h3><div id="financeCashLedger"></div></section>`;document.getElementById('adminConsole').appendChild(panel)}
async function loadFinanceAdmin(){if(typeof AEOPS==='undefined'||!AEOPS.token)return;mountFinanceAdmin();const err=document.getElementById('financeAdminError');try{const d=await opsQuery('finance.adminOverview',null,true);AEFINANCE.overview=d;AEFINANCE.adminLoaded=true;if(err)err.classList.add('hidden');renderFinanceAdmin(d)}catch(e){if(err){err.className='notice error';err.textContent=e.message}}}
function renderFinanceAdmin(d){const settings=d.settings||{},riders=d.riders||[],settlements=d.settlements||[],cash=d.recentCash||[];const held=riders.reduce((s,r)=>s+Math.max(0,Number(r.cash_balance||0)),0),paid=settlements.filter(x=>x.status==='paid').reduce((s,x)=>s+Number(x.payable_amount||0),0),approved=settlements.filter(x=>x.status==='approved').reduce((s,x)=>s+Number(x.payable_amount||0),0);document.getElementById('financeKpis').innerHTML=[["Rider cash recorded as held",finMoney(held)],["Approved restaurant payable",finMoney(approved)],["Paid settlements",finMoney(paid)],["Settlement policy",settings.ready?'Ready':'Not configured']].map(([a,b])=>`<div class="finance-kpi"><small>${a}</small><strong>${finEsc(b)}</strong></div>`).join('');const f=document.getElementById('financeSettingsForm');if(f){f.settlementsEnabled.checked=Boolean(settings.settlements_enabled);f.restaurantCommissionPercent.value=settings.restaurant_commission_percent??'';f.commissionBasis.value=settings.commission_basis||'';f.discountFunder.value=settings.discount_funder||'';f.restaurantReceivesDeliveryFee.value=settings.restaurant_receives_delivery_fee===true?'yes':settings.restaurant_receives_delivery_fee===false?'no':''}document.getElementById('financePolicyState').innerHTML=settings.ready?`<b>Policy ready.</b> ${Number(settings.restaurant_commission_percent||0)}% commission · ${finEsc(settings.commission_basis)} · discounts funded by ${finEsc(settings.discount_funder)} · delivery fee to restaurant: ${settings.restaurant_receives_delivery_fee?'yes':'no'}`:'Settlement creation remains blocked until the policy is complete and enabled.';const restaurantSelect=document.querySelector('#settlementPreviewForm [name=restaurantId]');if(restaurantSelect){const current=restaurantSelect.value;restaurantSelect.innerHTML='<option value="">Select restaurant…</option>'+((d.restaurants||[]).map(r=>`<option value="${finEsc(r.id)}">${finEsc(r.name)}${r.district?` — ${finEsc(r.district)}`:''}</option>`).join(''));restaurantSelect.value=current}document.getElementById('financeRiders').innerHTML=riders.length?riders.map(renderFinanceRider).join(''):'<p class="muted">No riders yet.</p>';document.getElementById('financeSettlements').innerHTML=settlements.length?settlements.map(renderFinanceSettlement).join(''):'<p class="muted">No settlements created yet.</p>';document.getElementById('financeCashLedger').innerHTML=cash.length?`<table class="finance-table"><thead><tr><th>Date</th><th>Rider</th><th>Type</th><th>Order</th><th>Amount</th><th>Reference</th></tr></thead><tbody>${cash.map(x=>`<tr><td>${finDate(x.created_at)}</td><td>${finEsc(x.rider_name)}</td><td>${finEsc(x.entry_type)}</td><td>${finEsc(x.order_number||'—')}</td><td>${finMoney(x.amount)}</td><td>${finEsc(x.reference||'—')}</td></tr>`).join('')}</tbody></table>`:'<p class="muted">No cash ledger entries yet.</p>'}
function renderFinanceRider(r){const balance=Number(r.cash_balance||0);return`<div class="finance-row"><div class="finance-row-head"><div><b>${finEsc(r.full_name)}</b><div class="finance-meta"><span>${finEsc(r.phone||'')}</span><span>${finEsc(r.vehicle||'')}</span><span>${r.total_deliveries||0} deliveries</span></div></div><div><b class="${balance>0?'finance-positive':'finance-zero'}">${finMoney(balance)}</b><small style="display:block">${balance>0?'cash awaiting reconciliation':'no cash due'}</small></div></div><div class="finance-actions"><button class="btn btn-light btn-sm" onclick="toggleFinanceRider('${finEsc(r.id)}','deposit')">Record cash deposit</button><button class="btn btn-light btn-sm" onclick="toggleFinanceRider('${finEsc(r.id)}','adjust')">Adjustment</button></div><form id="finance-deposit-${finEsc(r.id)}" class="finance-inline-form hidden" onsubmit="recordFinanceDeposit(event,'${finEsc(r.id)}')"><input name="amount" type="number" min="0.01" step="0.01" required placeholder="Amount received (AFN)"><input name="reference" required maxlength="120" placeholder="Receipt / reference"><input class="wide" name="notes" maxlength="800" placeholder="Notes (optional)"><button class="btn btn-primary wide" type="submit">Record deposit</button></form><form id="finance-adjust-${finEsc(r.id)}" class="finance-inline-form hidden" onsubmit="recordFinanceAdjustment(event,'${finEsc(r.id)}')"><input name="amount" type="number" step="0.01" required placeholder="Signed adjustment (+ / - AFN)"><input name="reference" required maxlength="120" placeholder="Adjustment reference"><input class="wide" name="notes" required minlength="3" maxlength="800" placeholder="Reason for adjustment"><button class="btn btn-primary wide" type="submit">Record audited adjustment</button></form></div>`}
window.toggleFinanceRider=function(id,type){document.getElementById(`finance-${type}-${id}`)?.classList.toggle('hidden')}
window.recordFinanceDeposit=async function(e,riderId){e.preventDefault();const d=new FormData(e.currentTarget);try{await opsMutation('finance.adminRecordRiderDeposit',{riderId,amount:Number(d.get('amount')),reference:String(d.get('reference')||''),notes:String(d.get('notes')||'')},true);e.currentTarget.reset();await loadFinanceAdmin()}catch(err){alert(err.message)}}
window.recordFinanceAdjustment=async function(e,riderId){e.preventDefault();const d=new FormData(e.currentTarget);try{await opsMutation('finance.adminRiderAdjustment',{riderId,amount:Number(d.get('amount')),reference:String(d.get('reference')||''),notes:String(d.get('notes')||'')},true);e.currentTarget.reset();await loadFinanceAdmin()}catch(err){alert(err.message)}}
window.saveFinanceSettings=async function(e){e.preventDefault();const f=e.currentTarget,d=new FormData(f),delivery=String(d.get('restaurantReceivesDeliveryFee')||'');try{await opsMutation('finance.adminUpdateSettings',{settlementsEnabled:Boolean(d.get('settlementsEnabled')),restaurantCommissionPercent:d.get('restaurantCommissionPercent')===''?null:Number(d.get('restaurantCommissionPercent')),commissionBasis:String(d.get('commissionBasis')||'')||null,discountFunder:String(d.get('discountFunder')||'')||null,restaurantReceivesDeliveryFee:delivery===''?null:delivery==='yes'},true);await loadFinanceAdmin()}catch(err){alert(err.message)}}
window.previewFinanceSettlement=async function(e){e.preventDefault();const d=new FormData(e.currentTarget),out=document.getElementById('financeSettlementPreview');try{const input={restaurantId:String(d.get('restaurantId')||''),startDate:String(d.get('startDate')||''),endDate:String(d.get('endDate')||'')};const p=await opsQuery('finance.adminSettlementPreview',input,true);AEFINANCE.preview={...p,input};out.innerHTML=`<div class="finance-preview"><div class="finance-row-head"><div><b>${finEsc(p.restaurant?.name||'Restaurant')}</b><div class="finance-meta"><span>${finEsc(input.startDate)} → ${finEsc(input.endDate)}</span><span>${p.totals.orderCount} orders</span></div></div><strong>${finMoney(p.totals.payableAmount)}</strong></div><div class="finance-kpis"><div class="finance-kpi"><small>Food gross</small><strong>${finMoney(p.totals.grossFood)}</strong></div><div class="finance-kpi"><small>Discounts</small><strong>${finMoney(p.totals.promoDiscounts)}</strong></div><div class="finance-kpi"><small>Commission</small><strong>${finMoney(p.totals.commissionAmount)}</strong></div><div class="finance-kpi"><small>Delivery fee component</small><strong>${finMoney(p.totals.deliveryFeeComponent)}</strong></div></div><div class="finance-policy"><span class="finance-chip">${p.policy.restaurantCommissionPercent}% commission</span><span class="finance-chip">${finEsc(p.policy.commissionBasis)}</span><span class="finance-chip">discount: ${finEsc(p.policy.discountFunder)}</span></div><div class="finance-warning">This is a server-calculated preview only. Creating it will create a <b>draft</b>; no payment is marked until a separate approval and paid action.</div><button class="btn btn-primary" onclick="createFinanceSettlement()">Create draft settlement</button></div>`}catch(err){AEFINANCE.preview=null;out.innerHTML=`<div class="notice error">${finEsc(err.message)}</div>`}}
window.createFinanceSettlement=async function(){const p=AEFINANCE.preview;if(!p)return;if(!confirm('Create this draft settlement from the current preview?'))return;try{await opsMutation('finance.adminCreateSettlement',{...p.input},true);AEFINANCE.preview=null;document.getElementById('financeSettlementPreview').innerHTML='<div class="notice success">Draft settlement created.</div>';await loadFinanceAdmin()}catch(err){alert(err.message)}}
function renderFinanceSettlement(s){const actions=s.status==='draft'?`<button class="btn btn-primary btn-sm" onclick="setFinanceSettlement('${finEsc(s.id)}','approved')">Approve</button><button class="btn btn-light btn-sm" onclick="setFinanceSettlement('${finEsc(s.id)}','cancelled')">Cancel</button>`:s.status==='approved'?`<button class="btn btn-primary btn-sm" onclick="setFinanceSettlement('${finEsc(s.id)}','paid')">Mark paid</button><button class="btn btn-light btn-sm" onclick="setFinanceSettlement('${finEsc(s.id)}','cancelled')">Cancel</button>`:'';return`<div class="finance-row"><div class="finance-row-head"><div><b>${finEsc(s.reference)} · ${finEsc(s.restaurant_name)}</b><div class="finance-meta"><span>${finDate(s.period_start)} → ${finDate(s.period_end)}</span><span>${s.order_count} orders</span><span>${finEsc(s.status)}</span></div></div><strong>${finMoney(s.payable_amount)}</strong></div><div class="finance-meta"><span>Food ${finMoney(s.gross_food)}</span><span>Commission ${finMoney(s.commission_amount)}</span><span>Delivery component ${finMoney(s.delivery_fee_component)}</span>${s.payment_reference?`<span>Payment ref: ${finEsc(s.payment_reference)}</span>`:''}</div><div class="finance-actions">${actions}</div></div>`}
window.setFinanceSettlement=async function(id,status){let paymentReference;if(status==='paid'){paymentReference=prompt('Enter the payment/bank/cash reference. This is required:')||'';if(!paymentReference)return}else if(!confirm(`${status==='approved'?'Approve':'Cancel'} this settlement?`))return;try{await opsMutation('finance.adminSetSettlementStatus',{id,status,paymentReference},true);await loadFinanceAdmin()}catch(err){alert(err.message)}}
function mountOwnerFinance(){if(typeof AEPortal==='undefined'||AEPortal.role!=='owner'||!AEPortal.token||document.getElementById('ownerFinanceCard'))return;const aside=document.querySelector('#ownerDashboard aside');if(!aside)return;const card=document.createElement('section');card.id='ownerFinanceCard';card.className='portal-card finance-portal-card';card.innerHTML=`<h2>${finText('Settlements','تسویه‌حساب‌ها')}</h2><p class="muted">${finText('View settlement statements created by Afghan Eats operations.','صورت‌حساب‌های ایجادشده توسط بخش عملیاتی افغان ایتس را مشاهده کنید.')}</p><div id="ownerFinanceBody"><span class="muted">${finText('Loading…','در حال بارگذاری…')}</span></div>`;aside.appendChild(card);loadOwnerFinance()}
async function loadOwnerFinance(){if(typeof AEPortal==='undefined'||AEPortal.role!=='owner'||!AEPortal.token)return;try{const d=await portalQuery('finance.ownerSummary');const el=document.getElementById('ownerFinanceBody');if(!el)return;el.innerHTML=`<div class="finance-kpi"><small>${finText('Delivered orders not yet in a settlement','سفارش‌های تحویل‌شده بدون تسویه')}</small><strong>${Number(d.unsettled?.order_count||0)}</strong><span class="muted">${finMoney(d.unsettled?.gross_food||0)} ${finText('food gross','فروش غذا')}</span></div><div class="finance-list" style="margin-top:10px">${(d.settlements||[]).length?(d.settlements||[]).map(s=>`<div class="finance-row"><div class="finance-row-head"><div><b>${finEsc(s.reference)}</b><div class="finance-meta"><span>${finDate(s.period_start)} → ${finDate(s.period_end)}</span><span>${finEsc(s.status)}</span></div></div><strong>${finMoney(s.payable_amount)}</strong></div>${s.payment_reference?`<small>${finText('Payment reference','مرجع پرداخت')}: ${finEsc(s.payment_reference)}</small>`:''}</div>`).join(''):`<p class="muted">${finText('No settlement statements yet.','هنوز صورت‌حسابی وجود ندارد.')}</p>`}</div>`}catch(e){const el=document.getElementById('ownerFinanceBody');if(el)el.innerHTML=`<div class="notice error">${finEsc(e.message)}</div>`}}
function mountRiderFinance(){if(typeof AEPortal==='undefined'||AEPortal.role!=='rider'||!AEPortal.token||document.getElementById('riderFinanceCard'))return;const aside=document.querySelector('#riderDashboard aside');if(!aside)return;const card=document.createElement('section');card.id='riderFinanceCard';card.className='portal-card finance-portal-card';card.innerHTML=`<h2>${finText('COD cash statement','صورت حساب نقدی')}</h2><p class="muted">${finText('A positive balance means Afghan Eats has recorded cash currently held by you and awaiting reconciliation. It is not your earnings.','موجودی مثبت یعنی مبلغ نقدی ثبت‌شده نزد شما که منتظر تسویه است؛ این مبلغ درآمد شما نیست.')}</p><div id="riderFinanceBody"><span class="muted">${finText('Loading…','در حال بارگذاری…')}</span></div>`;aside.appendChild(card);loadRiderFinance()}
async function loadRiderFinance(){if(typeof AEPortal==='undefined'||AEPortal.role!=='rider'||!AEPortal.token)return;try{const d=await portalQuery('finance.riderSummary'),el=document.getElementById('riderFinanceBody');if(!el)return;const bal=Number(d.rider?.cash_balance||0);el.innerHTML=`<div class="finance-balance ${bal>0?'finance-positive':'finance-zero'}">${finMoney(bal)}</div><small class="muted">${finText('Ledger begins with finance v12; older cash activity is not automatically reconstructed.','دفتر نقدی از نسخه مالی v12 آغاز می‌شود و فعالیت‌های قدیمی خودکار بازسازی نمی‌شود.')}</small><div class="finance-list" style="margin-top:12px">${(d.ledger||[]).length?(d.ledger||[]).map(x=>`<div class="finance-row"><div class="finance-row-head"><div><b>${finEsc(x.entry_type.replaceAll('_',' '))}</b><div class="finance-meta"><span>${finDate(x.created_at)}</span>${x.order_number?`<span>${finEsc(x.order_number)}</span>`:''}</div></div><strong>${finMoney(x.amount)}</strong></div><small>${finEsc(x.reference||'')}</small></div>`).join(''):`<p class="muted">${finText('No cash entries yet.','هنوز ثبت نقدی وجود ندارد.')}</p>`}</div>`}catch(e){const el=document.getElementById('riderFinanceBody');if(el)el.innerHTML=`<div class="notice error">${finEsc(e.message)}</div>`}}
function mountFinancePortal(){finStyles();mountOwnerFinance();mountRiderFinance()}
function initFinanceExperience(){finStyles();mountFinanceAdmin();mountFinancePortal();setInterval(()=>{mountFinanceAdmin();mountFinancePortal()},2500)}
document.addEventListener('DOMContentLoaded',initFinanceExperience);if(document.readyState!=='loading')initFinanceExperience();
